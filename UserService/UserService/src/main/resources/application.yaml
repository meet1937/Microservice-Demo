server:
  port: 8081

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/microservice
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: optional:configserver:http://localhost:8085
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-12858133.okta.com/oauth2/default
      client:
        registration:
          my-internal-client:
            provider: okta
            authorization-grant-type: client_credentials
            scope: internal
            client-id: 0oafrzurycBa5zP2J5d7
            client-secret: E4fy2uTedWYQz1k8VJ_xww46ds_-I_NaiXNVrmJKDTgsG7t_WkJAM0wyqATvXc8M
        provider:
          okta:
            issuer-uri: https://dev-12858133.okta.com/oauth2/default

okta:
  oauth2:
    issuer: https://dev-12858133.okta.com/oauth2/default
    audience: api://default


# Fetching from github config server
#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    prefer-ip-address: true